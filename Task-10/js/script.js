// write a JavaScript program for array reverse.

const array = [9, 8, 7, 6, 5, 4, 3, 2, 1, 0];
console.log(array);

// reversed array
const reverseArray = array.reverse();
console.log(`Reversd array is ${reverseArray}`);